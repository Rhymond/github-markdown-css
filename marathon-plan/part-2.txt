
    .table-wrap {
      overflow-x: auto;
      border: 1px solid var(--line);
      border-radius: 16px;
    }

    table {
      border-collapse: collapse;
      width: 100%;
      min-width: 1150px;
      background: white;
    }

    th, td {
      border-bottom: 1px solid var(--line);
      border-right: 1px solid var(--line);
      padding: 12px 13px;
      text-align: left;
      vertical-align: top;
      font-size: 13px;
    }

    th:last-child, td:last-child { border-right: 0; }
    tr:last-child td { border-bottom: 0; }

    th {
      position: sticky;
      top: 0;
      background: #f1f4f9;
      z-index: 1;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: .04em;
    }

    td.total {
      font-weight: 800;
      white-space: nowrap;
      background: #faf7f2;
    }

    tr.recovery td { background: #f8fbff; }
    tr.recovery td.total { background: #eef5ff; }
    tr.taper td { background: #fff9f2; }
    tr.taper td.total { background: #ffefd9; }

    ul, ol { padding-left: 20px; }
    li { margin: 6px 0; }

    .footer {
      text-align: center;
      color: var(--muted);
      padding: 0 20px 40px;
      font-size: 13px;
    }

    @media (max-width: 820px) {
      .hero-grid, .grid-2 { grid-template-columns: 1fr 1fr; }
      .section { padding: 20px; }
    }

    @media (max-width: 560px) {
      .hero { padding-top: 48px; }
      .hero-grid, .grid-2 { grid-template-columns: 1fr; }
      .wrap { width: min(100% - 20px, 1240px); }
      h2 { font-size: 24px; }
    }

    @media print {
      body { background: white; }
      .hero { background: #1f2937 !important; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .section { box-shadow: none; break-inside: avoid; }
      table { min-width: 0; }
      th, td { font-size: 8px; padding: 5px; }
      .table-wrap { overflow: visible; }
    }
  </style>
</head>
<body>
  <header class="hero">
    <div class="wrap">
      <div class="eyebrow">Marathon Training Plan</div>
      <h1>21 Weeks to Race Day</h1>
      <p>A balanced beginner-to-intermediate marathon program with easy running, tempo work, sprints, one weekly strength session, mobility training, long runs, recovery weeks and a focused two-week taper.</p>

      <div class="hero-grid">
        <div class="metric"><strong>21</strong><span>Training weeks</span></div>
        <div class="metric"><strong>4</strong><span>Run days per week</span></div>
        <div class="metric"><strong>1</strong><span>Strength session per week</span></div>
        <div class="metric"><strong>32 km</strong><span>Longest training run</span></div>
      </div>
    </div>
  </header>

  <main class="wrap">
    <section class="section">
      <h2>Who this plan is for</h2>
      <p>This program suits a runner who can already complete a comfortable 10 to 12 km run and currently runs roughly 20 to 25 km per week. The priority is to arrive healthy, prepared and confident rather than force an aggressive time target.</p>
      <div class="note"><strong>Important:</strong> easy days must remain easy. The plan works because hard days are separated by recovery, not because every run is fast.</div>
    </section>

    <section class="section">
      <h2>Weekly structure</h2>
      <div class="grid-2">
        <div class="mini-card">
          <span class="pill orange">Running</span>
          <ul>
            